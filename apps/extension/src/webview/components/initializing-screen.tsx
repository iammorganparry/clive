import { LoadingScreen } from "@clive/ui";

/**
 * Loading screen shown during app initialization.
 * Displayed while auth status and onboarding preferences are being loaded.
 */
export const InitializingScreen = () => <LoadingScreen />;
